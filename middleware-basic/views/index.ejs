<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs") %>
  <body>
  <!--「=」にすると特殊記号やHTMLタグがそのまま文字列になって出力してしまうため「-」にすることでエスケープできる -->
    <%- include("./partials/nav.ejs") %>
    <% const test="テスト" %>
    <%= test %>
    <h1 class="title">Home</h1>

    <% if(blogs.length > 0){ %>
        <% blogs.forEach(blog => { %>
          <h2><%= blog.title %></h2>
          <p><%= blog.content %></p>
        <% }) %>
    <% } else { %>
        <p>投稿はありません</p>
    <% } %>
  </body>
</html>
